{
  "name": "room-overlay-card",
  "version": "6.5.1",
  "description": "Home Assistant Lovelace card for room visualization with image overlay layers, conditional transitions, clickable zones, sliders, gauges, weather effects and embedded HA cards.",
  "keywords": [
    "home-assistant",
    "lovelace",
    "custom-card",
    "hacs"
  ],
  "license": "MIT",
  "scripts": {
    "test": "node tests/smoke.test.js && node tests/render.test.js && node tests/lifecycle.test.js",
    "test:smoke": "node tests/smoke.test.js",
    "test:lifecycle": "node tests/lifecycle.test.js",
    "test:e2e": "playwright test",
    "build": "node build.js --check",
    "build:verify": "node build.js --check && node tests/smoke.test.js dist/room-overlay-card.js && node tests/render.test.js dist/room-overlay-card.js && node tests/lifecycle.test.js dist/room-overlay-card.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "jsdom": "^30.0.1",
    "terser": "^5.50.0"
  }
}
