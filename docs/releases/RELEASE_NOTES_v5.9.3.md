# v5.9.3 — Title row rebalanced

## What's new

The version number moved next to "Room Overlay Card" instead of trailing the button row.
Undo, Redo, and the YAML toggle now form their own clearly right-aligned group, all sharing
a brighter, more visible border so they read as a set of controls rather than blending into
the background.

## Compatibility

No config changes — header layout polish only.

## Testing

2 render tests updated/added confirming button order and version placement. Full smoke and
render test suites pass.
